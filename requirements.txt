# Scientific Library RAG - Requirements
# Stack: FastAPI + LanceDB + BGE-M3 + Ollama

# === Web Framework (ASGI async) ===
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# === Vector Database (LanceDB - embedded, disk-native) ===
lancedb>=0.4.0
pyarrow>=14.0.0

# === LLM & Embeddings (Ollama + LangChain) ===
ollama>=0.1.0
langchain>=0.1.0
langchain-community>=0.0.10

# === PDF Processing ===
# PyMuPDF4LLM: 0.12s/doc, good for RAG
pymupdf4llm>=0.0.5
pymupdf>=1.23.0
pymupdf-layout>=1.26.0  # Improved page layout analysis
# GROBID client for structured metadata extraction
# (GROBID runs as Docker container)

# === HTTP Clients (async) ===
httpx>=0.26.0
aiohttp>=3.9.0

# === Data Models & Processing ===
pydantic>=2.5.0
pydantic-settings>=2.1.0
pandas>=2.1.0
numpy>=1.26.0

# === SQLite (async for metadata) ===
aiosqlite>=0.19.0

# === Caching ===
# Embedding cache: CacheBackedEmbeddings persists to disk
# Retrieval cache: TTL 30 minutes in memory
# Semantic cache: similarity > 0.95 threshold
cachetools>=5.3.0
diskcache>=5.6.0

# === Frontend ===
streamlit>=1.30.0
watchdog>=4.0.0  # Better file watching performance for Streamlit

# === Utilities ===
python-dotenv>=1.0.0
tenacity>=8.2.0
tqdm>=4.66.0
rich>=13.7.0

# === Optional: Nougat for equations (requires GPU ~4.2GB VRAM) ===
# nougat-ocr>=0.1.0
